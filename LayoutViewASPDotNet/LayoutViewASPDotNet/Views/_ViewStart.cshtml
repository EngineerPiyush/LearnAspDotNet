@{
    Layout = "_Layout";

    // We can also select the layout based on the user role or any other condition.For example, if we want to 
    // use a different layout for admin users, we can do something like this:
    // if(User.IsInRole("Admin"))
    // {
    //     Layout = "_Layout2";
    // }
    // else
    // {
    //     Layout = "_Layout";
    // }
}
